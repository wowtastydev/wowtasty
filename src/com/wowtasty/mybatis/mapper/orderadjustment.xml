<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orderadjustment">

	<sql id="columns"> 
		Seq, OrderID, RestaurantID, RegisterTime, AdjustType, AdjustItem, Price, UpdateID, UpdateTime
	</sql>

	<!-- Insert -->
	<insert id="insert" parameterType="orderadjustmentvo">
		INSERT INTO 
			order_adjustment
		(   
		<include refid="columns"/>
		) 
		VALUE (
			#{seq},
			#{orderID},
			#{restaurantID},
			#{registerTime},
			#{adjustType},
			TRIM(#{adjustItem}),
			#{price},
			#{updateID},
			NOW())
	</insert>
	
	<!--Delete-->
	<delete id = "delete" parameterType="String">
		DELETE FROM order_adjustment
		WHERE seq = #{seq}
	</delete>
</mapper>